const generate = require('../utils/generate');

vars = {
	iType: 'Exisiting Theme Installation',
	name: 'bootflow theme',
	proxy: 'wplocal.test'
};

test('two plus two is four', () => {
	expect(2 + 2).toBe(4);
});
